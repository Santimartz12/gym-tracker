import 'package:flutter/animation.dart';
import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ColorsApp {
  final backgroundColor = Color.fromARGB(255, 10, 10, 10);
  final primaryColor = Color(0xFFFEE600);
  final primaryColorOverlay = Color.fromARGB(30, 130, 102, 0);
  final navBarIcons = Color.fromRGBO(0, 0, 0, 1);
  final fontsLightColor = Color.fromARGB(255, 255, 255, 255);
  final fontsDarkColor = Color.fromARGB(255, 0, 0, 0);
  final lightColor = Color.fromARGB(255, 255, 255, 255);
  final doneColor = Color.fromARGB(255, 15, 225, 92);
}
